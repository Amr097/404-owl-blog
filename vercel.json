{
    "buildCommand": "cd client && npm install && serve -s build",
    "outputDirectory": "client/build",
    "rewrites": [
        {"source":"/api/(.*)",
          "distination" : "/api/api.js" }
    ]
}